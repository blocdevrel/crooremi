# Remifi Web demo (TanStack Start + Vite)
# Copy to web/.env.local:
#   cp .env.example .env.local

# Public vars only — safe to expose in browser (no secrets here)
VITE_BASE_RPC_URL=https://sepolia.base.org
VITE_BASE_CHAIN_ID=84532
VITE_USDC_ADDRESS=

# After contract deploy (Phase 2)
VITE_POLICY_REGISTRY_ADDRESS=
VITE_PAYMENT_ROUTER_ADDRESS=

VITE_AGENT_STORE_URL=
VITE_BASESCAN_URL=https://sepolia.basescan.org

# Optional: agent health endpoint for demo UI
VITE_AGENT_HEALTH_URL=http://localhost:3001/health
