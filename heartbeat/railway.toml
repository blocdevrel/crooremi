[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Every 5 minutes UTC (Railway minimum) — denser x402 traffic bursts
cronSchedule = "*/5 * * * *"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
startCommand = "/bin/sh /railway-heartbeat.sh"
