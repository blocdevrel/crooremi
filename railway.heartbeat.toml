[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.heartbeat"

[deploy]
# Every 20 minutes UTC (Railway minimum interval is 5 minutes).
cronSchedule = "*/20 * * * *"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
startCommand = "/bin/sh /railway-heartbeat.sh"
